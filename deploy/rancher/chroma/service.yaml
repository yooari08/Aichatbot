apiVersion: v1
kind: Service
metadata:
  name: chroma
  namespace: ai-chatbot
  labels:
    app.kubernetes.io/name: chroma
    app.kubernetes.io/component: vector-db
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: chroma
